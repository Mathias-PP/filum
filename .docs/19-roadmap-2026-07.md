# Roadmap consolidée — point au 2026-07-19

> Fusionne l'état de `STATE.md`, `13-audit-2026-05-26-followups.md`,
> `14-philum-rename-migration.md`, `16-declencheur-adoption.md` et
> `18-linked-accounts.md` en une seule vue priorisée. `06-roadmap.md` reste la
> roadmap produit d'origine (vision) ; ce fichier est la vue opérationnelle.

---

## Fait (rappel rapide)

- MVP complet end-to-end : login Google → création → sources → signature/attestation (ADR-019) → publication → fiche publique + graphe D3.
- Prod migrée **GCP e2-micro + Supabase** (ADR-028), HTTPS DuckDNS/Caddy, Vercel front.
- Chaîne extraction URL : Crossref → HTML/JSON-LD → LLM (LiteLLM, PRs #117-#119) + garde-fous titre (PR #121).
- Waitlist, claim de fiche v1, serveur MCP read-only (PRs #112-#114).
- Identité visuelle v1 (logo Pulsar-graph) + sandbox customizer.
- En attente de merge : #122 (multi-liens), #123 (modes couleur graphe), cette PR docs.

---

## P0 — Opérations immédiates (< 1 h, manuelles)

| Tâche | Détail |
|---|---|
| Alerte budget GCP 1 € | Billing → Budgets & alerts (pas de plafond natif sur GCP) |
| Redéployer la VM | `git pull` + `docker compose -f docker-compose.micro.yml up -d --build` pour livrer #116-#123 en prod |
| Décommissionner Railway | Supprimer le service + retirer l'ancienne redirect URI du client OAuth Google |
| Dependabot | 4 alertes sur main (2 high) — trier et bumper |

## P1 — Produit court terme (1-2 semaines)

| Tâche | Effort | Pourquoi |
|---|---|---|
| **Comptes liés v0** (liens déclaratifs) | S-M | Hub d'identité créateur, cf. `18-linked-accounts.md` |
| **F2** Tests intégration `POST /cards/{id}/publish` | S | Le path qui a coûté 4 PRs en mai |
| **F1** `openapi-typescript` | M | Prévient le drift back/front (types TS générés) |
| **F4** `POST /cards/{id}/restore` | S | Annuler un soft-delete |
| `impact_factor` : rebrancher ou retirer du UI | S | Toujours `null` depuis le retrait d'OpenAlex |

## P2 — Adoption (déclencheur : premier ambassadeur)

| Tâche | Effort | Déclencheur |
|---|---|---|
| Comptes liés v1 (vérification backlink YouTube) | M | Un créateur YouTube actif |
| Domaine custom `philum.app` | S | Premier ambassadeur prêt |
| Import Zotero / BibTeX / Obsidian | M-L | Demande créateur |
| OAuth YouTube (scope incrémental) + import vidéos | L | Ambassadeur YouTube confirmé |
| Onboarding waitlist → invitations | S | > 10 inscrits waitlist |

## P3 — Robustesse (déclencheurs techniques)

| Tâche | Déclencheur |
|---|---|
| **F5** Queue Wayback durable (Postgres + worker) | > 50 sources/jour (aujourd'hui : `asyncio.create_task` perdu au restart) |
| **F3** Tests sur Postgres au lieu de SQLite | Premier index partial / colonne JSONB |
| Test composant Svelte 5 (testing-library) | Au prochain composant critique |
| Rename Philum phases 2-4 (repo, URLs, emails) | Convertir en issues GitHub |
| Migration `006_remove_card_signature` (nettoyage ADR-019) | Prochaine migration de schéma |

## P4 — Long terme (conditionnel à validation produit)

- **Axe A** — Stockage cloud R2 du contenu original (dépend des interviews créateurs).
- **Axe B** — Archivage multi-cible (Wayback → Archive.today → Playwright).
- **F8** — Multi-tenancy si pivot B2B.
- Plugin navigateur + intégration CMS (couche de citation du web, cf. `00-vision.md`).
- Comptes liés v2 complet (Meta/TikTok OAuth) — seulement sur demande.

---

## Règle de mise à jour

Ce fichier est un **snapshot daté**. Quand une ligne est livrée, la déplacer
dans « Fait » avec le numéro de PR ; quand les priorités changent, bumper la
date du titre. `STATE.md` garde la vue 1-page ; ici on garde l'exhaustivité.
