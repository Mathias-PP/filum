# Spot-check lot 2 — seed=20260825, 2026-08-25
> Pour chaque item : comparer l'affirmation de la doc au code réel ci-dessous.
> Cocher `[x]` OK, ou écrire `CONTRADICTION` + fichier concerné (il retombera à statut=lu).

## - [x] apps/backend/app/services/agent_sessions.py:279
```


async def mettre_a_jour(
    db: AsyncSession,
    creator_id: UUID,
    session_id: UUID,
    *,
```

## - [x] apps/backend/app/services/agent.py:106
```


def _est_contexte_sature(erreur: str) -> bool:
    """L'erreur du fournisseur dit-elle que la fenêtre de contexte a débordé ?"""
    minuscule = erreur.lower()
    return any(marqueur in minuscule for marqueur in _MARQUEURS_CONTEXTE_SATURE)

```

## - [x] apps/backend/app/services/agent_sessions.py:155
```


async def creer(
    db: AsyncSession,
    creator_id: UUID,
    *,
    title: str = "",
```

## - [x] apps/backend/app/services/agent.py:979
```
    if retires:
        messages[:] = compactes
        await emit({"type": "contexte_compacte", "payload": {"messages_retires": retires}})

    usage_total: dict[str, int] = {"prompt_tokens": 0, "completion_tokens": 0}
    try:
        # Utiliser le quota_tours si défini dans agent_def, sinon utiliser MAX_TOURS
```

## - [x] apps/backend/app/services/agent.py:627
```


def _nettoyer_messages(
    messages: list[dict[str, Any]],
    noms_outils_valides: set[str] | None = None,
    provider_kind: str | None = None,
) -> list[dict[str, Any]]:
```

## - [x] apps/backend/app/services/agent_approvals.py:61
```


async def attendre(request_id: str, creator_id: UUID, *, delai: float = DELAI_MAX) -> bool:
    """Suspend jusqu'à la réponse humaine. Rend ``False`` au-delà du délai."""
    future = enregistrer(request_id, creator_id)
    try:
        return await asyncio.wait_for(future, timeout=delai)
```

