# Spot-check lot 3 — seed=20260825, 2026-08-25
> Pour chaque item : comparer l'affirmation de la doc au code réel ci-dessous.
> Cocher `[x]` OK, ou écrire `CONTRADICTION` + fichier concerné (il retombera à statut=lu).

## - [x] apps/backend/app/mcp_server/tools_write.py:150
```


async def _source_du_createur(db: AsyncSession, user: User, source_id: str) -> Source:
    """La source `source_id`, si elle appartient a une fiche du createur.

    Meme discretion que `_fiche_du_createur` : on refuse de dire « existe mais
    pas a vous », qui revelerait indirectement l'existence de sources d'autrui.
```

## - [x] apps/backend/app/mcp_server/auth.py:30
```


async def _user_depuis_token(db: AsyncSession, token: str | None) -> User | None:
    if not token:
        return None
    try:
        # `verify_aud=False` : les tokens du flow OAuth portent `aud: "mcp"`,
```

## - [x] apps/backend/app/mcp_server/tools.py:4
```

Fonctions pures (session en parametre) pour rester testables sans le
protocole MCP. Reponses volontairement compactes : l'IA cliente ne charge
que les noeuds qu'elle visite (frugalite en tokens).
"""

from __future__ import annotations
```

## - [x] tools_write.py:1295
```
(fichier illisible : tools_write.py)
```

## - [x] apps/backend/app/mcp_server/tools.py:39
```


def _arete_vers_une_fiche(source: Source) -> dict[str, str] | None:
    """L'adresse de la fiche Philum que cette source designe, si elle est ouverte.

    C'est l'unique arete fiche -> fiche, celle qui fait du corpus un graphe
    plutot qu'une collection de listes. Sans elle, un agent lisant une
```

## - [x] apps/backend/app/mcp_server/auth.py:89
```


async def exiger_utilisateur(db: AsyncSession) -> User:
    """Meme chose, mais leve une ToolError si personne n'est identifie.

    Le message pointe l'endroit ou l'agent va chercher son token : sans cette
    indication, il verrait juste « unauthorized » et n'aurait aucune piste.
```

