# Spot-check lot 1 — seed=20260825, 2026-08-25
> Pour chaque item : comparer l'affirmation de la doc au code réel ci-dessous.
> Cocher `[x]` OK, ou écrire `CONTRADICTION` + fichier concerné (il retombera à statut=lu).

## - [x] apps/backend/app/services/llm.py:139
OK — doc (04-transport-llm.md) : 400 = schéma refusé → repli `json_object`. Code : `if r.status_code == 400:` + log « schéma refusé, repli json_object ».
```
                system,
            )
            if r.status_code == 400:
                logger.info("LLM %s: schéma refusé, repli json_object", alias)
                r = await poste(
                    modele,
                    {"type": "json_object"},
```

## - [x] apps/backend/app/services/llm.py:666
OK — doc : `classify_url_type` classifie source|promo|social|other, contexte adjacent aidant. Code : signature + docstring conformes.
```


async def classify_url_type(url: str, context: str = "") -> str | None:
    """Classifie une URL en source / promo / social / other via LLM.

    ``context`` : texte adjacent (une ou deux phrases). Aide fortement la
    classification. Vide accepte mais reduit la fiabilite.
```

## - [x] apps/backend/app/models/agent_session.py:15
OK — doc (01-donnees.md) : `_JSON_LIST` = JSON portable + variante JSONB PostgreSQL. Code : ligne exacte `JSON().with_variant(JSONB(), "postgresql")` avec commentaire concordant.
```

#: JSONB en PostgreSQL (indexable, typé), JSON portable ailleurs (SQLite des tests).
_JSON_LIST = JSON().with_variant(JSONB(), "postgresql")


class AgentSession(Base):
    """Une conversation entre un créateur et son agent BYOK."""
```

## - [x] apps/backend/app/services/llm_adapters.py:218
OK — doc : `_parse_anthropic_blocking` convertit /v1/messages → format OpenAI-like, map stop_reason. Code : définition + docstring conformes.
```


def _parse_anthropic_blocking(
    data: dict[str, Any],
) -> tuple[dict[str, Any], str | None, dict[str, Any]] | str:
    """Anthropic /v1/messages → format OpenAI-like."""
    content_blocks = data.get("content") or []
```

## - [x] apps/backend/app/core/config.py:110
OK — doc (02-config-env.md) : `agent_allow_local_providers` défaut `False`, self-hosted uniquement, jamais sur le SaaS. Code : commentaire lignes 110-111 + champ ligne 112. Ancre doc affinée de :110 vers :112 pendant le spot-check (aucune contradiction).
```
    agent_gratuit_daily_quota_messages: int = 30
    agent_gratuit_zai_api_key: str = ""
    # Self-hosted uniquement : autorise localhost comme base_url de provider.
    # Ne jamais mettre a True sur le SaaS Philum (VM distante != loopback user).
    agent_allow_local_providers: bool = False

    @field_validator("database_url", mode="before")
```

## - [x] apps/backend/app/schemas/agent_chat.py:86
OK — doc (03-schemas-api.md) : `AgentSessionRead` = sortie liste/détail avec `from_attributes=True`. Code : classe + ConfigDict conformes.
```


class AgentSessionRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: UUID
    title: str
```

